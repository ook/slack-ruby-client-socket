# frozen_string_literal: true

RSpec.describe Slack::Ruby::Client::Socket do
  it "has a version number" do
    expect(Slack::Ruby::Client::Socket::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
